{
  "name": "testing-for-api",
  "version": "1.0.0",
  "description": "for tsting api",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "build": "esbuild src/index.js --bundle --platform=node --outfile=dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.7"
  }
}
